# 개별 매물 수집과 시점 관리

`/listings`의 ‘매물 원문 불러오기 · 시점 이력’에서 네이버 개별 매물 링크를 조회한다. Playwright Chromium을 백그라운드 작업으로 실행하며 작업 상태는 기존 Redis 작업 관리자에 저장한다. 사용자당 요청 간격은 1분, 브라우저 작업 제한은 45초다.

## 사용자 흐름

1. 개별 매물 링크 입력 → 원문 수집·재확인.
2. 추출된 가격·전용면적·주소·원문 확인일을 검토한다. 알 수 없는 값은 추측하지 않는다.
3. 추출값을 등록 폼에 적용한다. 부동산 유형은 직접 선택·확인한다.
4. 사용자 확인 일시와 거래 상태를 직접 입력한 뒤 저장한다. 페이지가 보인다는 이유로 거래 가능을 자동 지정하지 않는다.
5. 거래 가능·최근 7일·법정동 연결 조건을 충족한 매매 매물만 후보로 저장한다.

## 시점과 상태의 의미

| 항목 | 의미 |
|---|---|
| requested_at | 브라우저 조회를 시작한 시각 |
| fetched_at | 해당 조회가 끝난 시각. 실패에도 기록 |
| first_seen_at / last_seen_at | 가격·전용면적을 식별한 최초/마지막 수집 완료 시각 |
| source_confirmed_date | 원문에 표시된 확인일 문자열. 수집 시각으로 대체하지 않음 |
| confirmed_at | 사용자가 검토하여 입력한 확인 시각. 기존 매물 저장 기준 |
| observed | 페이지 표시값을 식별함. 거래 가능 보증이 아님 |
| unavailable | 페이지가 없거나 원문이 종료됨. 거래 완료·철회 여부는 알 수 없음 |
| blocked / failed / parse_error | 접근 제한 / 실행·통신 실패 / 가격·전용면적 식별 실패 |

각 조회 결과는 `listing_observations`에 별도로 남긴다. 조회 실패·원문 없음으로 기존 매물 가격이나 상태를 변경하거나 레코드를 삭제하지 않는다. 가격 변경 전후의 추출값은 수집 이력에, 사용자가 저장한 가격 변경은 기존 `listing_revisions`에 보존된다.

사용자 확인보다 새로운 수집 기록이 있거나 사용자 확인 후 7일이 지나면 ‘재확인 필요’다. 최신 추천과 후보 저장에서 제외되며 사용자가 확인 일시·상태를 다시 저장해야 복귀한다. 7일은 제품의 표시 기준이지 실제 매물 유효기간이 아니다. 이미 저장한 케이스 후보·분석 결과는 당시 스냅샷이며 자동 갱신하지 않는다.

## 실행·검증

- 신규 마이그레이션: `alembic upgrade head` (`l1c2e3f4a567`). RAG·실거래 테이블 변경 없음.
- Python 의존성 설치 후 `python -m playwright install --with-deps chromium`이 필요하다. Dockerfile.backend에 포함되어 있으므로 이미지 재빌드가 필요하다.
- `POST /api/listings/collection/jobs`, `GET /api/listings/collection/jobs/{id}`, `GET /api/listings/collection/history?source_url=...`. 모두 로그인 필요, 작업·이력은 본인만 조회한다.
- `tests/test_listing_collection.py`: URL 제한, 금액 변환, 공급/전용 구분, 시점과 실패 상태, 사용자 격리.
- `python scripts/verify_listing_collection_browser.py`: 실제 Chromium과 가상 HTML로 추출 성공·원문 없음 경로를 검증한다. 실제 네이버 매물 추출 성공률 검증은 아니다.
- `scripts/verify_listing_import_browser.cjs`: 수집 API 고정 응답을 이용해 추출값 적용·거래 상태 미확인 기본값·확인 일시 수동 입력을 검증한다.

2026-09-23 격리 PostgreSQL·Redis에서 마이그레이션 적용, 백엔드 923개 테스트와 고정 입력 평가 33개 통과. 프론트엔드 타입 검사·린트·빌드, 위 두 브라우저 검증 스크립트 통과. 서비스 Docker 재빌드·운영 DB 적용은 수행하지 않았다.

## 현재 범위

개별 링크를 사용자가 요청할 때만 조회한다. 정기 재수집·지역 전체 수집·목록에서 사라진 매물 판정은 없다. 자동화된 접근 제한을 우회하지 않으며 실패하면 수동 확인·등록을 이용한다. 원문 화면 변경이나 식별 불가능한 표기는 `parse_error`로 남는다. 토지·비주거처럼 전용면적이 없는 유형은 현재 자동 추출 성공 조건을 충족하지 못하므로 수동 입력한다.

실행은 기존 작업 관리자의 인프로세스 스레드를 사용한다. Redis는 상태 공유용이며 워커 종료 시 작업을 다른 워커가 재개하지 않는다. 따라서 운영용 지속 수집에는 내구성 있는 작업 실행기와 예약 재수집 정책을 별도로 추가해야 한다.

실제 네이버의 예전 매물 링크 1건을 Chrome으로 조회했을 때 ‘페이지를 찾을 수 없습니다’를 확인했다. 현재 유효한 실제 매물에서 가격·면적 추출 성공 여부는 아직 확인하지 못했다. 이 상태를 운영 수집 완료나 실매물 데이터 확보로 설명하지 않는다.

## 2026-09-24 검증

- 격리 PostgreSQL·Redis에서 마이그레이션 후 백엔드 테스트 925개와 고정 입력 평가 33개 통과. 실제 Chromium과 가상 원문으로 추출 성공·원문 없음 경로, 실제 API와 프론트의 등록·시점 화면 흐름을 확인했다. 프론트엔드 빌드·린트 통과.
- 네이버가 과거 매물 상세에서 `/404`로 이동하는 경로를 Playwright가 막던 문제를 고쳤다. 실제 과거 매물 링크(`2529960609`)를 다시 조회해 `unavailable`로 기록됨을 확인했다. 기존 매물을 거래 완료로 변경하지 않는다.
- 네이버 상세 화면의 ‘매매가’와 가격이 다른 줄에 표시되는 형태, 공급면적과 전용면적이 별도 행인 형태를 파서에 반영하고 고정 입력으로 검증했다. 현재 유효한 네이버 매물의 추출 성공은 아직 실측하지 못했다. 검색엔진의 오래된 캐시를 현재 매물로 간주하지 않는다.
- 현재 서비스 컨테이너는 실행 중이지 않아 운영 DB·이미지에는 적용하지 않았다.

## 2026-09-24 실제 사이트 재확인

- Playwright로 네이버 공식 홈(`land.naver.com`)을 열어 서울 강남구를 선택했으나 `new.land.naver.com/404`로 이동했다. `fin.land.naver.com/`도 `financial.pstatic.net/404.html`로 이동했다. 이 실행 환경에서 현행 매물 상세 링크를 확보하지 못했다.
- 과거 상세 링크(`2529960609`)를 실제 Chromium과 Python 수집기에 넣으면 `unavailable`로 기록된다. 이는 조회 종료 화면을 올바르게 분류했다는 증거이며 현재 매물 가격·면적 수집 성공을 뜻하지 않는다.
- 따라서 현시점에 ‘네이버 실매물 자동 수집이 제대로 된다’고 판단할 근거는 없다. 실제 등록 가능한 매물 링크가 브라우저에서 열리는 환경에서 가격·전용면적·원문 확인일을 대조하는 검증이 필요하다. 그 전까지 수집 실패 시 수동 등록 경로를 이용한다.
